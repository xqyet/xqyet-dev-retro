import Administrator from '../services/Administrator';
console.warn("You aren't supposed to be here... /admin")
console.warn("You aren't supposed to be here... /admin")

fetch('/check-auth', {
    headers: {
        'Password': '67f707a9-e44c-8007-b076-487a7e28bb96'
    }
});